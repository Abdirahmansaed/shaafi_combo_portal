<x-layouts.app title="Subscriber Details">
    <a href="{{ route('subscribers.index') }}" class="text-sm">Back to subscribers</a>
    @if(session('success')) <div class="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div> @endif
    <div class="my-6 flex justify-between"><div><p class="text-sm text-slate-500">MSISDN</p><h2 class="text-2xl font-semibold">{{ $subscriber->msisdn }}</h2><p>{{ $subscriber->package }} · {{ \Carbon\Carbon::parse($subscriber->expiry_date)->format('d M Y, H:i') }}</p></div><x-status :value="$subscriber->status" /></div>
    <section class="overflow-hidden rounded-lg border bg-white"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-3">Package</th><th class="p-3">Purchase Date</th><th class="p-3">Expiry Date</th><th class="p-3">Price</th><th class="p-3">Subscription</th><th class="p-3">Agent Work</th><th class="p-3">Action</th><th class="p-3">Done By</th></tr></thead><tbody>@foreach($history as $p)<tr class="border-t"><td class="p-3">{{ $p->package }}</td><td class="p-3">{{ \Carbon\Carbon::parse($p->purchase_date)->format('d M Y, H:i') }}</td><td class="p-3">{{ \Carbon\Carbon::parse($p->expiry_date)->format('d M Y, H:i') }}</td><td class="p-3">{{ number_format($p->price,2) }}</td><td class="p-3"><x-status :value="$p->status" /></td><td class="p-3"><x-status :value="$p->agent_status" /></td><td class="p-3">@if($p->agent_status === 'PENDING')<form method="POST" action="{{ route('subscribers.complete', $p->id) }}">@csrf<button class="rounded bg-teal-700 px-2.5 py-1 text-xs font-medium text-white">Complete</button></form>@else<span class="text-xs font-medium text-emerald-700">Completed</span>@endif</td><td class="p-3">{{ optional(optional($p->action)->doneBy)->displayName() ?: '—' }}</td></tr>@endforeach</tbody></table><div class="border-t p-3">{{ $history->links() }}</div></section>
</x-layouts.app>
