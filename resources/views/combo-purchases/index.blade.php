<x-layouts.app title="Combo Purchases"><div class="mb-6"><p class="text-sm text-slate-500">Review valid Combo Package purchases.</p></div>@if(session('error'))<p class="mb-4 rounded-md bg-rose-50 p-3 text-sm text-rose-700">{{ session('error') }}</p>@endif<form class="mb-5 grid gap-3 rounded-lg border border-slate-200 bg-white p-4 lg:grid-cols-5" method="GET"><div class="lg:col-span-2"><label class="mb-1 block text-xs font-medium text-slate-500">Search MSISDN</label><input name="q" value="{{ request('q') }}" placeholder="MSISDN" class="w-full rounded-md border border-slate-300 p-2 text-sm"></div><div><select name="package" class="w-full rounded-md border border-slate-300 p-2 text-sm"><option value="">All Packages</option>@foreach(['Daily','Weekly','Monthly'] as $type)<option @selected(request('package')===$type)>{{ $type }}</option>@endforeach</select></div><div><select name="status" class="w-full rounded-md border border-slate-300 p-2 text-sm"><option value="">All statuses</option>@foreach(['Active','Expired'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div><div class="flex gap-2"><select name="range" class="min-w-0 flex-1 rounded-md border border-slate-300 p-2 text-sm"><option value="">Any date</option><option value="today" @selected(request('range') === 'today')>Today</option><option value="7" @selected(request('range') === '7')>Last 7 days</option><option value="30" @selected(request('range') === '30')>Last 30 days</option></select><button class="rounded-md bg-teal-700 px-3 text-white">Filter</button><a href="{{ route('combo-purchases.export-pdf', request()->query()) }}" class="whitespace-nowrap rounded-md border border-teal-700 px-3 py-2 text-sm font-medium text-teal-700">Export PDF</a></div></div></form><section class="overflow-hidden rounded-lg border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr>@foreach(['ID','MSISDN','Package','Purchase date','Expiry date','Price','Amount MB','Status','Action'] as $h)<th class="whitespace-nowrap px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody>@forelse($records as $p)<tr class="table-row border-t border-slate-100"><td class="px-4 py-4">#{{ $p->id }}</td><td class="px-4 py-4">{{ $p->msisdn }}</td><td class="px-4 py-4">{{ $p->package }}</td><td class="px-4 py-4">{{ \Carbon\Carbon::parse($p->purchase_date)->format('d M Y, H:i') }}</td><td class="px-4 py-4">{{ \Carbon\Carbon::parse($p->expiry_date)->format('d M Y, H:i') }}</td><td class="px-4 py-4">{{ number_format($p->price,2) }}</td><td class="px-4 py-4">{{ number_format($p->amount_mb) }}</td><td class="px-4 py-4"><x-status :value="$p->status"/></td><td class="px-4 py-4"><a href="{{ route('combo-purchases.show',$p->id) }}" class="text-teal-700">View</a></td></tr>@empty<tr><td colspan="9" class="px-4 py-12 text-center text-slate-500">No valid purchases match those filters.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 px-4 py-3">{{ $records->links() }}</div></section></x-layouts.app>
